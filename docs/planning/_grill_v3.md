# ✅ VitroVision — บันทึกการตัดสินใจ (Decision Log)

> เริ่มจาก Grill Session 2026-07-29 · เขียนใหม่ให้เหลือเฉพาะข้อสรุปที่ยังใช้ได้ + สถานะอัปเดตตามผลจริง
> รายละเอียดหลักฐานเต็มอยู่ใน `docs/DEV_LOG.md` + runbooks

## บรีฟตั้งต้น (2026-07-29 — ยังคงเป็นจริง)
1. **ทิ้ง Android app** → ทำเป็น **Web app** ที่ใช้งานได้จริง (ปัจจุบัน = HF Space Gradio)
2. **เปลี่ยนสิ่งที่ประเมิน:** จาก "ความพร้อม sub-culture" → **"ความพร้อมอนุบาล" (acclimatization readiness)** — การเปลี่ยนโจทย์จริง ไม่ใช่แค่เปลี่ยนคำเรียก
3. ปลายทาง = ประเมินความพร้อมของต้นที่ส่งต่อ (สำหรับคนในแล็บ/ผู้ใช้)

---

## 🔑 การตัดสินใจสำคัญ + สถานะปัจจุบัน

| # | การตัดสินใจ | สถานะ (อัปเดต) |
|---|---|---|
| 1 | **ประเมิน "ความพร้อมอนุบาล"** แทน sub-culture | ✅ ใช้แล้ว — เปลี่ยนระบบ verdict เป็น ยังไม่พร้อม/พร้อมอนุบาล/ตรวจเอง |
| 2 | **ใช้ Web app แทน Android** | ✅ ใช้แล้ว — HF Space Gradio |
| 3 | **ระบบราก = ตัวชี้วัดอันดับ 1** ของความพร้อมอนุบาล | ⚠️ ทดสอบแล้ว — มองรากผ่านขวดได้ไม่ดี (พบ 1/100) → **ใช้ height_proxy เป็นหลัก** (ข้อจำกัดที่ชัดเจน) |
| 4 | **rule-based hard threshold** (ไม่ใช่ weighted score) | ✅ ใช้แล้ว — อธิบายได้ ตรวจสอบได้ |
| 5 | **marketplace = vision ปลายทาง ไม่ใช่ scope รอบนี้** | ✅ ยึด — รอบนี้สร้างเครื่องมือวัด/ประเมิน เท่านั้น |
| 6 | ห้ามใช้ `coverage_ratio` ในสูตรตัดสิน (ความหมายกลับทิศ) | ✅ ยึด — แสดงเป็นข้อมูลได้ แต่ไม่ใช้ตัดสิน |
| 7 | ต้องมี **"ตรวจเอง"** เป็นทางออก (decision-support) | ✅ ยึด — manual override เสมอ |

## 📌 บทเรียนสำคัญ (จาก pivot ที่ผ่านมา)
- เปลี่ยนสถาปัตยกรรมไปเรื่อยจนไม่เข้าใจงานตัวเอง (บทเรียนจาก v1) → **ถามให้ชัดก่อนลงมือ**
- ตาคนเห็น ≠ โมเดล segment ได้ — ต้อง spike test จริงก่อนทำ feature ใหญ่
- ภาพ 2D ผ่านขวดแก้วมีข้อจำกัด → ชี้ว่าต้องเสริมข้อมูล 3 มิติ

## 📎 อ้างอิง
- การตัดสินใจเรื่อง feature/เกณฑ์: `docs/planning/_orchestration.md` + `_tool_matrix.md`
- ข้อจำกัด/ผลจริง: `docs/deliverables/proposal_ysc.md`, `docs/runbooks/*`
